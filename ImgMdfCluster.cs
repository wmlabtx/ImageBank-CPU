using OpenCvSharp;
using System;
using System.Diagnostics;
using System.Drawing;
using System.IO;

namespace ImageBank
{
    public partial class ImgMdf
    {
        /*

        var descriptors = ImageHelper.GetSift2Descriptors(bitmap);
                    if (descriptors == null) {
                        Assert.Fail();
                    }

        var vectors = new Mat[2];
        ectors[0] = ImgMdf.GetVector()
        */
    }
}