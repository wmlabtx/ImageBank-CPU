using System.Linq;

namespace ImageBank
{
    public static partial class ImgMdf
    {
        /*
        public static int GetRatingSize(int rating)
        {
            if (rating == 0) {
                return 0;
            }

            var size = _imgList.Count(e => e.Value.Rating == rating);
            return size;
        }

        public static void DescreaseRating()
        {
            var imgX = AppVars.ImgPanel[0].Img;
            var rating = imgX.Rating - 1;
            if (rating >= 0) {
                imgX.SetRating(rating);
            }
        }
        */
    }
}
